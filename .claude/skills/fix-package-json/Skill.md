---
name: fix package json
description: Fix Package JSON `devDependencies` and `dependencies` by sorting alphabetical order
---

## Rules

- Run `npm run lint:pkg-json` command and read the issue.
- Make change only on `devDependencies` and `dependencies` in `package.json` file.
- DO NOT change anything else.
